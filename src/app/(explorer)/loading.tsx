import { LoadingState } from "@/components/LoadingState/LoadingState";

// Renders the loading state for explorer routes.
export default function Loading() {
  return <LoadingState label="Loading category..." />;
}
